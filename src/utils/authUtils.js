export const verificarPermisoTabla = (permisos, modelo) => {
    // let nivelPermiso = 0;
    // if (permisos.length === 0) {
    //   nivelPermiso = 4;
    //   return nivelPermiso;
    // } else {
    //   if (permisos.includes(`view_${modelo}`)) {
    //     nivelPermiso++;
    //   }
    //   if (permisos.includes(`change_${modelo}`)) {
    //     nivelPermiso++;
    //   }
    //   if (permisos.includes(`add_${modelo}`)) {
    //     nivelPermiso++;
    //   }
    //   if (permisos.includes(`delete_${modelo}`)) {
    //     nivelPermiso++;
    //   }
    //   console.log(`modelo: ${modelo}, permisos: ${nivelPermiso}`);
    //   return nivelPermiso;
    // }
    return 4
  };